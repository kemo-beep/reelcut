# Server
PORT=8080
LOG_LEVEL=info
# Set DEBUG=1 or APP_ENV=development to include error details in 500 responses (e.g. register)
DEBUG=
APP_ENV=

# Database (PostgreSQL)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/reelcut?sslmode=disable
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=5m
DB_CONN_MAX_IDLE_TIME=10m

# Redis
REDIS_URL=redis://localhost:6379/0
# Enable API rate limiting (set to 1 in production; off by default so local dev avoids 429)
ENABLE_RATE_LIMIT=

# JWT
JWT_SECRET=change-me-in-production
JWT_REFRESH_SECRET=change-me-refresh
JWT_ACCESS_EXPIRY=1h
JWT_REFRESH_EXPIRY=168h

# Object storage: MinIO (S3-compatible). All uploads use MinIO. docker-compose maps MinIO to 9002 (API) and 9003 (console).
# Create bucket "reelcut" in MinIO console (http://localhost:9003) if needed.
# If the backend runs in Docker but the browser must reach MinIO on the host, set S3_PUBLIC_ENDPOINT to the same URL.
S3_ENDPOINT=http://localhost:9002
S3_PUBLIC_ENDPOINT=
S3_REGION=us-east-1
S3_BUCKET=reelcut
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin
S3_USE_PATH_STYLE=true

# Asynq (job queue)
ASYNQ_QUEUE=default
ASYNQ_CONCURRENCY=5

# Transcription: use WhisperLiveKit (WebSocket) or OpenAI.
# If TRANSCRIPTION_WS_URL is set, the Go backend uses the WhisperLiveKit ASR service instead of OpenAI.
# Run the Python service: cd backend/transcription_service && pip install -r requirements.txt && uvicorn app:app --host 0.0.0.0 --port 8000
TRANSCRIPTION_WS_URL=
# OpenAI (fallback when TRANSCRIPTION_WS_URL is empty)
OPENAI_API_KEY=

# AI clip suggestions: Gemini analyzes transcript and suggests viral clips (TikTok, Reels, Shorts).
# If set, POST .../suggest-clips uses Gemini; otherwise uses rule-based suggestions.
GEMINI_API_KEY=
# Optional; default is gemini-2.0-flash
GEMINI_MODEL=

# Stripe (subscriptions)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID_PRO=

# Email (password reset, verification). Leave SMTP_* empty to disable sending (no-op).
EMAIL_FROM=noreply@reelcut.local
FRONTEND_BASE_URL=http://localhost:5173
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_USE_TLS=true
EMAIL_TOKEN_SECRET=
EMAIL_RESET_EXPIRY=1h
EMAIL_VERIFY_EXPIRY=24h

# Optional: when set (e.g. 1 or true), completing a transcription automatically enqueues an auto-cut job.
AUTO_CUT_AFTER_TRANSCRIPTION=

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003,http://localhost:5173

# Optional: set SEED_DEV=true to run seed migration (dev only)
SEED_DEV=
